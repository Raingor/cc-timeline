{
    "name": "Auto Convert to WebP",
    "version": "1.0.0",
    "description": "自动将新添加的图片转换为WebP格式",
    "when": {
        "type": "fileCreated",
        "patterns": ["images/*.jpg", "images/*.jpeg", "images/*.png"]
    },
    "then": {
        "type": "runCommand",
        "command": "for f in $FILE; do cwebp -q 80 \"$f\" -o \"${f%.*}.webp\"; rm \"$f\"; done && echo 'Converted to WebP: $FILE'"
    }
}
